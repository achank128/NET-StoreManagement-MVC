@model ExportStore

@{
    ViewData["Title"] = "Detail Export Store";
}

<div class="row text">
    <div class="col-md-10 offset-1">
       <h2>Chi tiết Phiếu xuất kho</h2>
        <div class="row">
            <form asp-action="Create">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                <div class="form-group mb-2">
                    <label asp-for="ExporterName" class="control-label"></label>
                    <input asp-for="ExporterName" class="form-control" readonly />
                    <span asp-validation-for="ExporterName" class="text-danger"></span>
                </div>
                <div class="form-group mb-2">
                    <label asp-for="Customer" class="control-label"></label>
                    <input asp-for="Customer" class="form-control" readonly />
                    <span asp-validation-for="Customer" class="text-danger"></span>
                </div>
                <div class="form-group mb-2">
                    <label asp-for="ExportDate" class="control-label"></label>
                    <input asp-for="ExportDate" class="form-control" type="date" readonly />
                    <span asp-validation-for="ExportDate" class="text-danger"></span>
                </div>
                <div class="mb-2">
                    <p class="mb-0">Chi tiết phiếu xuất kho:</p>
                    <table id="table" class="table table-bordered">
                        <thead>
                            <tr>
                                <th>Sản phẩm</th>
                                <th>Đơn giá</th>
                                <th>Số lượng</th>
                                <th>Giá bán</th>
                            </tr>
                        </thead>
                        <tbody class="product-items">
                            @foreach (var item in Model.ExportStoreDetails)
                            {
                                <tr class="product-item">
                                    <td>
                                        <input asp-for="@item.Product.ProductName" class="form-control" readonly />
                                    </td>
                                    <td><input asp-for="@item.Product.Price" class="form-control" readonly /></td>
                                    <td><input asp-for="@item.Quantity" class="form-control" readonly /></td>
                                    <td><input asp-for="@item.ExportPrice" class="form-control" readonly /></td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
                <div class="form-group mb-4">
                    <label asp-for="Total" class="control-label"></label>
                    <input asp-for="Total" class="form-control" readonly />
                    <span asp-validation-for="Total" class="text-danger"></span>
                </div>
                <div class="form-group d-flex justify-content-between">
                    <a class="btn btn-outline-secondary" asp-action="Index">Quay lại</a>
                </div>
            </form>
        </div>
    </div>
</div>

